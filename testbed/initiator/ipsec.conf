# /etc/ipsec.conf — CipherLens Initiator Node
config setup
    charondebug="ike 2, knl 2, cfg 2"

conn %default
    keyexchange=ikev2
    ike=aes256gcm128-prfsha384-modp2048!
    esp=aes256gcm128!
    dpdaction=restart
    dpddelay=30s

conn site-to-site
    left=192.168.100.10
    leftid=@initiator.cipherlens.local
    leftauth=psk
    right=192.168.100.20
    rightid=@responder.cipherlens.local
    rightauth=psk
    auto=start
